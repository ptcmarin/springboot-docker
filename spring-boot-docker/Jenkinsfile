pipeline{
    agent any

    stages{
        stage("Build"){
            
            steps{
                dir("spring-boot-docker/"){
                    sh "mvn -version"
                    sh "mvn clean install"
                }
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
